{
  "name": "aegis-university-erp",
  "version": "1.0.0",
  "description": "Next-generation University ERP platform built with vanilla components.",
  "scripts": {
    "dev": "npx -y http-server -p 3000",
    "build": "npx -y vite build",
    "preview": "npx -y vite preview --port 3000"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
